# Port targets — `unicode-segmentation-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `unicode-segmentation` that codex-rs uses, and therefore the minimum subset that `unicode-segmentation-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-177] (rank `116`, tier `direct`, status `To Do`)

**Cargo package name**: `unicode-segmentation`
**Rust module name (in `use` statements)**: `unicode_segmentation`

## Summary

- **1** distinct symbols imported by codex-rs
- **4** total `use` statements
- **4** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `unicode_segmentation::UnicodeSegmentation`

- **4 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/bottom_pane/textarea.rs:34` (`codex-tui`) — `use unicode_segmentation::UnicodeSegmentation`
- `tui/src/chatwidget.rs:372` (`codex-tui`) — `use unicode_segmentation::UnicodeSegmentation`
- `tui/src/history_cell.rs:98` (`codex-tui`) — `use unicode_segmentation::UnicodeSegmentation`
- `tui/src/text_formatting.rs:1` (`codex-tui`) — `use unicode_segmentation::UnicodeSegmentation`
