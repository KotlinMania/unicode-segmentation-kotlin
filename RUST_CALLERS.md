# Rust callers — `unicode-segmentation-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `unicode-segmentation` | `unicode_segmentation` | 10 | 5 | 4 | 4 |

## `unicode-segmentation`

### Inbound symbols (1 distinct, 10 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `unicode-segmentation`.

#### `unicode_segmentation::UnicodeSegmentation` — 10 site(s) (4 codex, 6 other, 5 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/bottom_pane/textarea.rs:34` — `use unicode_segmentation::UnicodeSegmentation`
- ✅ `codex-kotlin/codex-tui` `src/chatwidget.rs:372` — `use unicode_segmentation::UnicodeSegmentation`
- ✅ `codex-kotlin/codex-tui` `src/history_cell.rs:98` — `use unicode_segmentation::UnicodeSegmentation`
- ✅ `codex-kotlin/codex-tui` `src/text_formatting.rs:1` — `use unicode_segmentation::UnicodeSegmentation`
- `bm25-kotlin/bm25` `src/default_tokenizer.rs:268` — `use unicode_segmentation::UnicodeSegmentation`
- `nucleo-kotlin/nucleo-matcher` `src/chars.rs:150` — `use unicode_segmentation::UnicodeSegmentation`
- `ratatui-kotlin/ratatui-core` `src/buffer/buffer.rs:5` — `use unicode_segmentation::UnicodeSegmentation`
- `ratatui-kotlin/ratatui-core` `src/text/line.rs:1252` — `use unicode_segmentation::UnicodeSegmentation`
- `ratatui-kotlin/ratatui-core` `src/text/span.rs:4` — `use unicode_segmentation::UnicodeSegmentation`
- `ratatui-kotlin/ratatui-widgets` `src/reflow.rs:10` — `use unicode_segmentation::UnicodeSegmentation`
