# Port report — `unicode-segmentation-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `unicode-segmentation`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 10 `use` sites from 4 other repos
- **Outbound**: 0 `use` sites to 0 other repos
- **Unresolved external**: 2 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `unicode-segmentation` | `unicode_segmentation` | 10 | 0 | COD-177 | 116 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (10 edges, 5 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-tui` | 4 | ✅ |
| `ratatui-kotlin` | `ratatui-core` | 3 |  |
| `bm25-kotlin` | `bm25` | 1 |  |
| `nucleo-kotlin` | `nucleo-matcher` | 1 |  |
| `ratatui-kotlin` | `ratatui-widgets` | 1 |  |

### Symbol-level demand (1 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `unicode_segmentation::UnicodeSegmentation` | 10 | 4 | 6 | 5 |

#### Per-symbol detail (top 1 symbols)

##### `unicode_segmentation::UnicodeSegmentation` (10 sites, 4 codex, 6 other, 5 caller crates)

Codex call sites:

- `codex-kotlin/codex-tui` `src/bottom_pane/textarea.rs:34` — `use unicode_segmentation::UnicodeSegmentation`
- `codex-kotlin/codex-tui` `src/chatwidget.rs:372` — `use unicode_segmentation::UnicodeSegmentation`
- `codex-kotlin/codex-tui` `src/history_cell.rs:98` — `use unicode_segmentation::UnicodeSegmentation`
- `codex-kotlin/codex-tui` `src/text_formatting.rs:1` — `use unicode_segmentation::UnicodeSegmentation`

Other workspace call sites:

- `bm25-kotlin/bm25` `src/default_tokenizer.rs:268` — `use unicode_segmentation::UnicodeSegmentation`
- `nucleo-kotlin/nucleo-matcher` `src/chars.rs:150` — `use unicode_segmentation::UnicodeSegmentation`
- `ratatui-kotlin/ratatui-core` `src/buffer/buffer.rs:5` — `use unicode_segmentation::UnicodeSegmentation`
- `ratatui-kotlin/ratatui-core` `src/text/line.rs:1252` — `use unicode_segmentation::UnicodeSegmentation`
- `ratatui-kotlin/ratatui-core` `src/text/span.rs:4` — `use unicode_segmentation::UnicodeSegmentation`
- `ratatui-kotlin/ratatui-widgets` `src/reflow.rs:10` — `use unicode_segmentation::UnicodeSegmentation`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

_No outbound edges to other workspace repos._

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `criterion` | 385 | 11 | `unicode-segmentation` |
| `quickcheck` | 27 | 8 | `unicode-segmentation` |
